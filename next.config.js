/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig

// images with api.pierrette-essentielle.co

module.exports = {
    images: {
        domains: ['api.pierrette-essentielle.com'],
    }
}